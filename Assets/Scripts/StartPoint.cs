using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This script exists to have a moving object collide with an object that contains this script, and it is assigned as the "Start Point" of its travel. 

public class StartPoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
